[project]
name = "resume-generator"
version = "0.1.0"
description = "Script to generate PDF resume from Markdown"
requires-python = ">=3.10"
dependencies = [
    "markdown>=3.0",
    "weasyprint>=60.0",
]
